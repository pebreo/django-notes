'use strict';

angular.module('homeDetail').
    component('homeDetail', {
        templateUrl: '/api/templates/test.html',
        controller: function(
                $cookies,
                $http,
                $location,
                $routeParams,
                $rootScope,
                $scope
            ){}
})
